import { SeatshowtimeviewModel } from './seatshowtimeview.model';

describe('SeatshowtimeviewModel', () => {
  it('should create an instance', () => {
    expect(new SeatshowtimeviewModel()).toBeTruthy();
  });
});
