export interface UserProfileViewModel {
  id: string;
  firstName: string;
  lastName: string;
  address?: string;
  dateOfBirth?: Date;
  gender: string;
  identityCard: string;
  avatar?: string;
  email: string;
  phoneNumber: string;
  username: string;
}